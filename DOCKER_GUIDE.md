# Руководство по Docker

## 🚀 Быстрый старт

### Разработка (Development)

```powershell
# Сборка и запуск всех контейнеров
npm run docker:up

# Проверка статуса
docker ps

# Просмотр логов
npm run docker:logs

# Остановка
npm run docker:down
```

### Production

```powershell
# Запуск production версии
npm run docker:prod
```

## 📋 Команды Docker

### Разработка

- `npm run docker:up` - Запуск контейнеров с автоматической сборкой
- `npm run docker:rebuild` - Пересборка и перезапуск контейнеров
- `npm run docker:down` - Остановка и удаление контейнеров
- `npm run docker:logs` - Просмотр логов в реальном времени

### База данных

- `npm run docker:init-db` - Инициализация схемы базы данных
- `npm run docker:fix-tables` - Исправление отсутствующих таблиц
- `npm run docker:check-tables` - Проверка существующих таблиц
- `npm run docker:psql` - Подключение к PostgreSQL через psql

### Production

- `npm run docker:prod` - Запуск production версии

## 🔧 Устранение проблем

### Проблема: Модуль 'pg' не найден

**Решение:** Пересоберите Docker образ:

```powershell
npm run docker:rebuild
```

Или вручную:
```powershell
docker-compose -f docker-compose.simple.yml build --no-cache
docker-compose -f docker-compose.simple.yml up -d
```

### Проблема: Порт 3000 занят

**Решение:** Остановите локальный dev сервер или измените порт в `docker-compose.simple.yml`:

```yaml
ports:
  - "3001:3000"  # Внешний порт 3001, внутренний 3000
```

### Проблема: База данных не подключается

**Решение:** Проверьте:

1. Контейнер базы данных запущен:
   ```powershell
   docker ps | Select-String "supabase"
   ```

2. Переменные окружения правильные:
   ```powershell
   docker exec fb-net-nextjs env | Select-String "DATABASE_URL"
   ```

3. Сеть Docker создана:
   ```powershell
   docker network ls | Select-String "fb-net"
   ```

### Проблема: Изменения не применяются

**Решение:** В режиме разработки файлы монтируются, но для применения изменений в зависимостях нужно пересобрать:

```powershell
npm run docker:rebuild
```

## 📁 Структура Docker файлов

- `Dockerfile.dev` - Development образ (hot-reload, исходники монтируются)
- `Dockerfile` - Production образ (оптимизированная сборка)
- `docker-compose.simple.yml` - Development композиция (PostgreSQL + Next.js)
- `docker-compose.prod.yml` - Production композиция

## 🌐 Доступ к приложению

После запуска:
- **Приложение**: http://localhost:3000
- **База данных**: localhost:54322

## ⚙️ Конфигурация

### Переменные окружения

В `docker-compose.simple.yml` настроены:
- `DATABASE_URL` - подключение к PostgreSQL (использует имя сервиса `supabase`)
- `NEXT_PUBLIC_SUPABASE_URL` - для совместимости (не используется в текущей реализации)
- `NODE_ENV=development` - режим разработки

### Volumes

- `.:/app` - монтирование исходного кода для hot-reload
- `/app/node_modules` - исключение node_modules (используется из образа)
- `/app/.next` - исключение .next (генерируется в контейнере)

## 🔐 Безопасность

⚠️ **ВАЖНО для Production:**

1. Измените пароль PostgreSQL в `docker-compose.prod.yml`
2. Используйте секреты для переменных окружения
3. Настройте firewall и ограничьте доступ к портам
4. Используйте HTTPS (настройте reverse proxy)

## 📚 Полезные команды

```powershell
# Просмотр логов конкретного сервиса
docker logs fb-net-nextjs -f

# Выполнение команды в контейнере
docker exec -it fb-net-nextjs sh

# Перезапуск сервиса
docker-compose -f docker-compose.simple.yml restart nextjs-app

# Очистка неиспользуемых образов и контейнеров
docker system prune -a
```

