# Реорганизация скриптов деплоя

Дата: 2026-02-04

## 📊 Что было изменено

### ❌ Удалены ненужные скрипты (11 файлов):
1. `deploy-and-migrate-bun.ps1` - миграция на Bun уже выполнена
2. `deploy-and-migrate-bun.sh` - миграция на Bun уже выполнена
3. `deploy-app-only.ps1` - дублирующий функционал (rsync)
4. `deploy-app-only.sh` - дублирующий функционал (rsync)
5. `deploy-to-server.ps1` - дублирующий функционал (rsync)
6. `deploy-to-server.sh` - дублирующий функционал (rsync)
7. `setup-ssl.sh` - разовая операция
8. `check-logs.sh` - не критичный
9. `security-check.sh` - не критичный
10. `backup-database.sh` - оставлен только PowerShell вариант
11. `restore-database.sh` - восстановление выполняется вручную

### ✅ Оставлены только необходимые (3 PowerShell скрипта):
1. **commit-and-push.ps1** - коммит и push в GitHub
2. **deploy-from-github.ps1** - деплой с GitHub (обновлен!)
3. **backup-database.ps1** - ручной бэкап БД

### 🔄 Обновлен главный скрипт:
**deploy-from-github.ps1** - добавлен параметр `-AppOnly` для быстрого деплоя без пересборки БД

---

## 🚀 Новая система деплоя

### Вариант 1: Быстрый деплой (используйте в 90% случаев)

```powershell
# 1. Коммит и push
.\scripts\commit-and-push.ps1 -Message "Обновил компонент"

# 2. Быстрый деплой (только приложение)
.\scripts\deploy-from-github.ps1 -AppOnly
```

**Преимущества:**
- ⚡ Быстро (~2-3 минуты)
- 🔒 База данных не останавливается
- 💾 Нет простоя БД

---

### Вариант 2: Полный деплой (редко, при миграциях)

```powershell
# 1. Коммит и push
.\scripts\commit-and-push.ps1 -Message "Добавил миграцию БД"

# 2. Полный деплой (все контейнеры)
.\scripts\deploy-from-github.ps1
```

**Когда использовать:**
- Добавлены миграции БД
- Изменилась структура БД
- Первый деплой на сервер

---

## 📝 Параметры deploy-from-github.ps1

| Параметр | Описание | Пример |
|----------|----------|--------|
| (без параметров) | Полный деплой | `.\scripts\deploy-from-github.ps1` |
| `-AppOnly` | **Только приложение** (БД работает) | `.\scripts\deploy-from-github.ps1 -AppOnly` |
| `-SkipBackup` | Без бэкапа БД | `.\scripts\deploy-from-github.ps1 -SkipBackup` |
| `-Branch dev` | Из другой ветки | `.\scripts\deploy-from-github.ps1 -Branch dev` |
| `-Init` | Первоначальная настройка | `.\scripts\deploy-from-github.ps1 -Init` |

---

## 🎯 Сравнение: До и После

### До реорганизации:
- **14 скриптов** в папке scripts/ (mix PowerShell + Bash)
- Дублирующий функционал (rsync + git pull)
- Неясно какой скрипт использовать
- Необходимость помнить какой скрипт для чего

### После реорганизации:
- **3 PowerShell скрипта** (только необходимые)
- Единый скрипт деплоя с GitHub
- Простой выбор: `-AppOnly` или полный деплой
- Понятная документация
- Только PowerShell для Windows

---

## 📚 Документация

1. **scripts/README.md** - краткая справка по скриптам
2. **DEPLOY_GUIDE.md** - полное руководство по деплою
3. **SCRIPTS_REORGANIZATION.md** - этот документ

---

## ⏱️ Время деплоя

| Команда | До | После |
|---------|-----|-------|
| Обновление кода | ~5-7 мин (все контейнеры) | ~2-3 мин (только app) ✅ |
| С миграциями БД | ~5-7 мин | ~5-7 мин (без изменений) |

**Выигрыш:** ~3-4 минуты на каждом деплое кода!

---

## ✅ Что изменилось в рабочем процессе

### Старый процесс:
```powershell
# Коммит
git add .
git commit -m "..."
git push

# Деплой (перезапуск всех контейнеров)
.\scripts\deploy-to-server.ps1 -Server root@server
```

### Новый процесс:
```powershell
# Коммит (автоматически)
.\scripts\commit-and-push.ps1 -Message "..."

# Быстрый деплой (только приложение)
.\scripts\deploy-from-github.ps1 -AppOnly
```

**Быстрее и проще!** 🚀

---

## 🔧 Troubleshooting

Если что-то не работает после деплоя:

```bash
# Подключитесь к серверу
ssh root@your-server.com

# Проверьте статус
cd /opt/fb-net
docker compose -f docker-compose.production.yml ps

# Логи приложения
docker compose -f docker-compose.production.yml logs app --tail=50

# Перезапуск только приложения
docker compose -f docker-compose.production.yml restart app
```

---

## 📞 Поддержка

См. подробную документацию:
- [DEPLOY_GUIDE.md](./DEPLOY_GUIDE.md) - полное руководство
- [scripts/README.md](./scripts/README.md) - справка по скриптам
