# Быстрый старт - Полная инициализация проекта

## 🚀 Один скрипт для всего

Для полной автоматической инициализации проекта выполните:

```bash
npm run setup
```

Этот скрипт автоматически выполнит:
1. ✅ Проверку переменных окружения
2. ✅ Выполнение SQL схемы базы данных
3. ✅ Миграцию данных из `news-data.ts` в Supabase
4. ✅ Создание новостей из папок с фотографиями

## 📋 Пошаговая инструкция

### Шаг 1: Выберите способ запуска Supabase

**Вариант A: Supabase CLI (рекомендуется для разработки)**

```bash
# Установить Supabase CLI
npm install -g supabase

# Инициализировать и запустить локальный Supabase
npm run setup:supabase

# Скопируйте переменные из вывода команды в .env.local
```

**Вариант B: Docker Compose**

```bash
# Запустить Docker контейнеры и инициализировать БД
npm run setup:docker

# Переменные окружения создадутся автоматически
```

**Вариант C: Облачный Supabase**

1. Создайте проект на [supabase.com](https://supabase.com)
2. Перейдите в Settings → API
3. Скопируйте URL и anon key
4. Создайте `.env.local`:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

### Шаг 2: Выполните полную настройку

```bash
npm run setup
```

Скрипт автоматически:
- Проверит подключение к Supabase
- Выполнит SQL схему
- Мигрирует все данные
- Создаст новости из папок

### Шаг 3: Запустите приложение

```bash
npm run dev
```

Откройте http://localhost:3000

## ✅ Проверка работы

1. Откройте страницу `/news` - должны отображаться новости из базы данных
2. Проверьте фильтрацию по годам и категориям
3. Откройте любую новость - должна загружаться из Supabase

## 🔄 Обновление кода

Компоненты автоматически используют Supabase, если он доступен, иначе fallback на статические данные из `news-data.ts`.

Это позволяет:
- ✅ Работать без базы данных на этапе разработки
- ✅ Автоматически переключаться на Supabase после настройки
- ✅ Не ломать приложение при проблемах с подключением

## 📝 Что происходит при `npm run setup`

1. **Проверка окружения** - наличие переменных Supabase
2. **Выполнение SQL схемы** - создание таблиц в базе
3. **Миграция данных** - перенос из `news-data.ts` в Supabase
4. **Создание новостей** - из папок с фотографиями

## 🐛 Устранение проблем

### Переменные окружения не найдены
```bash
# Создайте .env.local вручную
# Для Supabase CLI - скопируйте из вывода supabase start
# Для Docker - используйте значения из docker-compose.simple.yml
```

### Ошибка подключения к базе
```bash
# Проверьте, что Supabase запущен
supabase status  # для CLI
docker ps        # для Docker

# Проверьте переменные окружения
cat .env.local
```

### Данные не загружаются
```bash
# Проверьте наличие данных в базе
npm run docker:psql
# или через Supabase Studio: http://localhost:54323
```

## 📚 Дополнительная информация

- Подробная документация: [DEVELOPMENT.md](./DEVELOPMENT.md)
- Структура БД: `supabase-schema.sql`
- Скрипты: папка `scripts/`

