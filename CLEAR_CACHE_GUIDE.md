# 🧹 Руководство по очистке кешей

## Автоматическая очистка (рекомендуется)

### На локальной машине (Windows)

Запустите скрипт для автоматической очистки всех кешей проекта:

```powershell
.\scripts\clear-all-caches.ps1
```

Этот скрипт очищает:
- ✅ Next.js кеш (`.next`)
- ✅ Turbopack кеш (`.turbo`)
- ✅ npm кеш
- ✅ bun кеш
- ✅ Docker кеши (если Docker запущен)
- ✅ TypeScript build info файлы
- ✅ ESLint кеш
- ✅ Vercel кеш

### На сервере (Linux)

Запустите скрипт для очистки всех кешей на сервере:

```bash
# Просто очистка кешей
chmod +x scripts/clear-server-caches.sh
./scripts/clear-server-caches.sh

# Очистка кешей + пересборка и перезапуск контейнеров
./scripts/clear-server-caches.sh --rebuild
```

Скрипт для сервера очищает:
- ✅ Docker контейнеры (остановка)
- ✅ Docker образы (неиспользуемые)
- ✅ Docker тома (неиспользуемые)
- ✅ Docker build кеш
- ✅ nginx кеши
- ✅ Next.js кеши внутри контейнеров
- ✅ Локальные кеши проекта (`.next`, `.turbo`, `.eslintcache`)

**Важно:** Скрипт автоматически определяет, какая конфигурация используется (SSL или обычная) и применяет соответствующий docker-compose файл.

## Ручная очистка

### 1. Next.js кеш

```powershell
# Удалить папку .next
Remove-Item -Recurse -Force .next

# Или через npm
npm run build -- --no-cache
```

### 2. node_modules (полная переустановка)

```powershell
# Удалить node_modules
Remove-Item -Recurse -Force node_modules

# Переустановить зависимости
npm install
# или
bun install
```

### 3. Docker кеши

```powershell
# Остановить контейнеры
docker-compose down

# Очистить все неиспользуемые ресурсы
docker system prune -a --volumes

# Или по отдельности:
docker image prune -a -f      # Удалить все неиспользуемые образы
docker container prune -f     # Удалить остановленные контейнеры
docker volume prune -f        # Удалить неиспользуемые тома
docker builder prune -a -f    # Очистить build кеш
```

### 4. npm кеш

```powershell
npm cache clean --force
```

### 5. bun кеш

```powershell
bun pm cache rm
```

## Кеши браузера

### Chrome/Edge
1. Нажмите `Ctrl + Shift + Delete`
2. Выберите "Изображения и файлы в кеше"
3. Нажмите "Удалить данные"

Или для конкретной страницы:
- `Ctrl + F5` - жесткая перезагрузка
- `Ctrl + Shift + R` - перезагрузка с очисткой кеша

### Firefox
1. Нажмите `Ctrl + Shift + Delete`
2. Выберите "Кеш"
3. Нажмите "Удалить сейчас"

Или для конкретной страницы:
- `Ctrl + F5` - жесткая перезагрузка
- `Ctrl + Shift + R` - перезагрузка с очисткой кеша

## Кеши социальных сетей (Open Graph)

После изменения метаданных (og:image, og:title и т.д.) нужно очистить кеш в соцсетях:

### Facebook
1. Перейдите на: https://developers.facebook.com/tools/debug/
2. Введите URL вашей страницы
3. Нажмите "Scrape Again" (Обновить)

### Twitter/X
1. Перейдите на: https://cards-dev.twitter.com/validator
2. Введите URL вашей страницы
3. Нажмите "Preview card"

### LinkedIn
1. Перейдите на: https://www.linkedin.com/post-inspector/
2. Введите URL вашей страницы
3. Нажмите "Inspect"

### Telegram
Telegram кеширует превью ссылок. Для очистки:
1. Отправьте ссылку боту @webpagebot
2. Или подождите 24-48 часов (кеш обновляется автоматически)

### VK (ВКонтакте)
1. Перейдите на: https://vk.com/dev/utils.getLinkStats
2. Используйте API метод или подождите обновления кеша

## После очистки кешей

1. **Пересоберите проект:**
   ```powershell
   npm run build
   ```

2. **Перезапустите dev сервер:**
   ```powershell
   npm run dev
   ```

3. **Если используете Docker:**
   ```powershell
   docker-compose up -d --build
   ```

## Быстрая команда для полной очистки и пересборки

### На локальной машине:
```powershell
.\scripts\clear-all-caches.ps1; npm run build
```

### На сервере:
```bash
# Очистка + пересборка + перезапуск
./scripts/clear-server-caches.sh --rebuild

# Или по отдельности:
./scripts/clear-server-caches.sh
docker-compose -f docker-compose.ssl.yml up -d --build
```

## Проверка после очистки

После очистки кешей проверьте:
- ✅ Сайт открывается корректно
- ✅ Изображения загружаются
- ✅ Open Graph превью отображается правильно в соцсетях
- ✅ Нет ошибок в консоли браузера
