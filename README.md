# FB.NET - Веб-сайт медицинского оборудования

## 📋 Описание

FB.NET - современный веб-сайт компании, специализирующейся на поставке и обслуживании медицинского оборудования для вакуумной аспирационной биопсии (ВАБ) молочной железы.

## ⚡ Простой запуск сайта

**Чтобы просто запустить сайт локально:**

```bash
npm run dev
```

Сайт будет доступен по адресу: **http://localhost:3000**

> **Примечание:** Для полной функциональности (новости, база данных) может потребоваться настройка Supabase (см. раздел "Быстрый старт" ниже).

## 🚀 Быстрый старт

### Полная автоматическая инициализация

**Один скрипт запустит всё:**
```bash
npm run setup
```

Этот скрипт автоматически:
1. ✅ Проверит переменные окружения
2. ✅ Выполнит SQL схему базы данных
3. ✅ Мигрирует данные из `news-data.ts` в Supabase
4. ✅ Создаст новости из папок с фотографиями

**После выполнения скрипта приложение автоматически будет использовать базу данных!**

### Варианты запуска Supabase

**Вариант 1: Supabase CLI (рекомендуется)**
```bash
# Установить Supabase CLI
npm install -g supabase

# Инициализировать и запустить
npm run setup:supabase

# Выполнить полную настройку
npm run setup
```

**Вариант 2: Docker Compose**
```bash
# Запустить контейнеры и инициализировать БД
npm run setup:docker

# Выполнить полную настройку
npm run setup
```

**Вариант 3: Облачный Supabase**
1. Создайте проект на [supabase.com](https://supabase.com)
2. Выполните SQL схему из `supabase-schema.sql` в SQL Editor
3. Создайте `.env.local` с вашими ключами
4. Запустите: `npm run setup`

### Переменные окружения

Создайте `.env.local` в корне проекта:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Telegram Bot (опционально)
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_ADMIN_CHAT_ID=your_admin_chat_id
TELEGRAM_WEBHOOK_URL=https://your-domain.com/api/telegram/webhook

# OpenRouter для AI расширения текста (опционально)
OPENROUTER_API_KEY=your_openrouter_api_key

# Сайт URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

**Для Supabase CLI**: Ключи появятся в выводе команды `supabase start`  
**Для Docker**: Используйте значения из `docker-compose.simple.yml`  
**Для облачного**: Скопируйте из Settings → API в Supabase Dashboard

## 🛠 Технологии

- **Framework**: Next.js 15 (App Router)
- **Язык**: TypeScript
- **Стилизация**: Tailwind CSS + Кастомные CSS классы
- **UI компоненты**: Radix UI (shadcn/ui)
- **База данных**: Supabase (PostgreSQL)
- **Развертывание**: Vercel/Netlify

## 📁 Структура проекта

```
fb.net/
├── src/
│   ├── app/                    # Next.js страницы
│   ├── components/            # Переиспользуемые компоненты
│   ├── lib/                    # Утилиты и данные
│   └── styles/                 # Кастомные стили
├── scripts/                     # Утилитарные скрипты
│   ├── setup.ts               # Главный скрипт инициализации
│   ├── migrate-news-to-supabase.ts
│   ├── create-news-from-folders.ts
│   └── organize-images-by-date.ts
├── supabase/                   # Конфигурация Supabase
├── docker-compose.simple.yml   # Docker конфигурация
└── supabase-schema.sql         # SQL схема БД
```

## 🔧 Доступные команды

### Разработка
```bash
npm run dev              # Запуск dev сервера
npm run build            # Сборка для продакшна
npm run start            # Запуск продакшн сервера
npm run lint             # Проверка кода
```

### Инициализация и настройка
```bash
npm run setup                    # Полная автоматическая инициализация
npm run setup:supabase          # Инициализация Supabase CLI
npm run setup:docker            # Запуск Docker Compose + инициализация БД
```

### Docker
```bash
npm run docker:up        # Запустить контейнеры
npm run docker:down      # Остановить контейнеры
npm run docker:logs      # Просмотр логов
npm run docker:psql      # Подключиться к PostgreSQL
npm run docker:init-db   # Инициализировать БД
```

### Утилиты
```bash
npm run migrate:news              # Мигрировать новости в Supabase
npm run organize:images          # Организовать изображения по датам
npm run rename:folders           # Переименовать папки с датами
npm run create:news-from-folders # Создать новости из папок
```

## 📄 Основные страницы

- `/` - Главная страница
- `/patients` - Информация для пациентов
- `/equipment` - Описание оборудования
- `/training` - Программы обучения
- `/news` - Новости и события
- `/conferences` - Конференции
- `/contacts` - Контакты и форма обратной связи

## 📊 База данных

Проект использует **Supabase** (PostgreSQL) для хранения:
- Новостей
- Изображений
- Видео
- Документов
- Тегов

### Схема базы данных

Выполните SQL из `supabase-schema.sql` для создания всех таблиц.

### Миграция данных

После создания таблиц выполните:
```bash
npm run migrate:news              # Из news-data.ts
npm run create:news-from-folders # Из папок с фотографиями
```

## 🎨 Стилизация

Проект использует гибридный подход:
- **Tailwind CSS** - для утилитарных классов
- **Кастомные CSS классы** - для повторяющихся компонентов (`src/styles/components.css`)

---

## 🤖 Telegram Bot для создания новостей

Бот позволяет автоматически создавать новости через Telegram, отправляя текст, изображения и видео.

### Настройка бота

1. **Создайте бота через BotFather:**
   - Откройте [@BotFather](https://t.me/botfather) в Telegram
   - Отправьте `/newbot` и следуйте инструкциям
   - Сохраните полученный токен

2. **Получите Chat ID администратора:**
   - Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
   - Отправьте любое сообщение
   - Скопируйте ваш Chat ID

3. **Получите API ключ OpenRouter:**
   - Зарегистрируйтесь на [openrouter.ai](https://openrouter.ai)
   - Создайте API ключ в настройках
   - Скопируйте ключ

4. **Добавьте переменные в `.env.local`:**
   ```env
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   TELEGRAM_ADMIN_CHAT_ID=your_chat_id_here
   OPENROUTER_API_KEY=your_openrouter_key_here
   TELEGRAM_WEBHOOK_URL=https://your-domain.com/api/telegram/webhook
   ```

5. **Настройте webhook:**
   ```bash
   npm run setup:telegram-bot
   ```

### Использование бота

1. Откройте вашего бота в Telegram
2. Отправьте `/start` для начала работы
3. Отправьте текст новости, фотографии или видео
4. Отправьте `/done` для завершения создания новости
5. Бот создаст черновик и отправит уведомление администратору
6. Администратор может опубликовать или отклонить новость через кнопки в Telegram

### Команды бота

- `/start` - Начать работу с ботом
- `/done` - Завершить создание новости
- `/cancel` - Отменить создание новости

---

## 📦 Развертывание

### Vercel (рекомендуется)
1. Подключить репозиторий GitHub
2. Настроить переменные окружения
3. Автоматическая сборка и деплой

### Netlify
1. Build command: `npm run build`
2. Publish directory: `.next`
3. Настроить переменные окружения

## 📚 Дополнительная документация

- **[QUICK_START.md](./QUICK_START.md)** - Быстрый старт с пошаговой инструкцией
- **[DEVELOPMENT.md](./DEVELOPMENT.md)** - Подробная документация для разработчиков

## 📞 Контакты

- **Email**: info@fb.net
- **Телефон**: +7 (495) 123-45-67
- **Сайт**: https://fb.net

## 📄 Лицензия

Все права защищены © 2025 FB.NET
