# Отчёт о проверке безопасности сервера
**Дата:** 17 февраля 2026  
**Сервер:** 155.212.217.60 (figmpptenw)  
**Время работы:** 8 дней, 6:57

---

## ✅ Результат проверки

### Процесс пользователя 1001 — НЕ вредоносный

**Найденный процесс:**
- **PID:** 226079
- **Пользователь:** 1001 (nextjs)
- **Команда:** `bun node server-with-logging.js`
- **Контейнер:** fb-net-app (b0cf914009a7)
- **Потребление:** 40% памяти (2.4 ГБ), 12% CPU (накопленное за 6 дней)

**Вывод:** Это ваше собственное Next.js приложение, запущенное в Docker-контейнере под непривилегированным пользователем `nextjs` (UID 1001). Это стандартная практика безопасности Docker.

---

## 🔒 Обнаруженные проблемы безопасности

### 1. SSH был уязвим для брутфорса

**До исправления:**
- `PermitRootLogin yes` — разрешён вход root
- Аутентификация по паролю была включена
- **7,562** неудачных попыток входа в логах
- **31,827** попыток заблокировано fail2ban
- **3,461** IP-адресов заблокировано

**Топ атакующих IP:**
1. 103.191.209.229 — 621 попытка
2. 91.202.233.33 — 580 попыток
3. 176.120.22.47 — 495 попыток
4. 14.225.2.104 — 245 попыток
5. 176.120.22.13 — 173 попытки

---

## ✅ Применённые исправления

### 1. Усилена защита SSH

**Изменения в `/etc/ssh/sshd_config`:**
```
PermitRootLogin prohibit-password  # Только SSH-ключи для root
PasswordAuthentication no          # Отключена аутентификация по паролю
PubkeyAuthentication yes           # Разрешены только SSH-ключи
```

**Результат:**
- ✅ Конфигурация проверена: `sshd -t` — OK
- ✅ SSH перезагружен успешно
- ✅ Подключение по ключу работает
- ✅ Теперь невозможно войти по паролю

### 2. Усилена конфигурация fail2ban

**Новые параметры в `/etc/fail2ban/jail.local`:**
```
[DEFAULT]
bantime = 1h          # Бан на 1 час по умолчанию
findtime = 10m        # Окно наблюдения 10 минут
maxretry = 3          # Максимум 3 попытки

[sshd]
enabled = true
maxretry = 3          # 3 неудачных попытки
bantime = 3h          # Бан на 3 часа для SSH
findtime = 10m        # За 10 минут
```

**Результат:**
- ✅ fail2ban перезапущен
- ✅ Сейчас заблокировано: 4 IP
- ✅ Более строгие правила применены

---

## 🔍 Дополнительные проверки

### Проверка на вредоносное ПО
- ✅ Криптомайнеры не найдены (проверены процессы: crypto, xmr, bitcoin, miner)
- ✅ Подозрительных файлов в `/tmp` и `/var/tmp` не обнаружено
- ✅ Все открытые порты соответствуют легитимным сервисам

### Открытые порты
| Порт | Процесс | Назначение |
|------|---------|------------|
| 22   | sshd    | SSH (теперь только ключи) |
| 80   | docker-proxy | HTTP (nginx) |
| 443  | docker-proxy | HTTPS (nginx) |
| 53   | systemd-resolved | DNS (локальный) |

### Docker-контейнеры
```
✅ fb-net-nginx    — работает 7 дней
✅ fb-net-app      — работает 7 дней (процесс 226079)
✅ fb-net-db       — работает 7 дней (healthy)
✅ fb-net-certbot  — работает 7 дней
```

---

## 📊 Текущее состояние сервера

- **Uptime:** 8 дней
- **Load average:** 0.37, 0.33, 0.28 (нормально)
- **Последний вход:** 17 февраля 07:39 с 91.244.168.148 (ваш IP)
- **Активные атаки:** Заблокированы fail2ban

---

## 🎯 Рекомендации

### Выполнено ✅
1. ✅ Отключена аутентификация по паролю
2. ✅ Усилены правила fail2ban
3. ✅ Проверено отсутствие вредоносного ПО

### Дополнительно (опционально)
1. **Сменить SSH-порт** с 22 на нестандартный (например, 2222) — уменьшит количество автоматических атак
2. **Настроить UFW firewall** для дополнительной защиты
3. **Включить автоматические обновления безопасности:**
   ```bash
   apt install unattended-upgrades
   dpkg-reconfigure -plow unattended-upgrades
   ```
4. **Настроить мониторинг** (например, Netdata или Prometheus)
5. **Регулярно проверять логи:**
   ```bash
   journalctl -u sshd -f
   fail2ban-client status sshd
   ```

---

## 📝 Заключение

**Сервер безопасен.** Процесс от пользователя 1001 оказался вашим собственным приложением в Docker. Основная уязвимость (SSH с паролем) устранена. Сервер находится под постоянной атакой ботов, но fail2ban успешно их блокирует.

**Критичных проблем не обнаружено.**
