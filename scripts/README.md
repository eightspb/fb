# Скрипты для работы с проектом

## 📋 Основные скрипты

### 1. commit-and-push.ps1
Коммит и push изменений в GitHub

```powershell
# Автоматический коммит всех изменений
.\scripts\commit-and-push.ps1

# С указанием сообщения
.\scripts\commit-and-push.ps1 -Message "Описание изменений"
```

---

### 2. deploy-from-github.ps1
Деплой проекта из GitHub на сервер

```powershell
# Быстрый деплой (только приложение, БД работает) - используйте это в 90% случаев
.\scripts\deploy-from-github.ps1 -AppOnly

# Полный деплой (все контейнеры, с миграциями БД)
.\scripts\deploy-from-github.ps1

# Первый запуск на новом сервере (клонирование репозитория)
.\scripts\deploy-from-github.ps1 -Init
```

**Параметры:**
- `-AppOnly` - пересобирается только приложение (БД продолжает работать)
- `-SkipBackup` - без создания бэкапа БД
- `-Branch dev` - деплой из другой ветки
- `-Init` - первоначальная настройка сервера

---

## 🤖 Telegram скрипты

### fix-telegram-now.sh (Быстрое исправление)

**Используйте это в первую очередь!** Автоматически исправляет все проблемы с Telegram ботом.

```bash
# На сервере
bash scripts/fix-telegram-now.sh
```

Что делает:
- ✅ Проверяет доступность endpoint
- ✅ Удаляет старый webhook
- ✅ Устанавливает новый webhook
- ✅ Очищает необработанные сообщения
- ✅ Проверяет установку
- ✅ Отправляет тестовое сообщение

### diagnose-telegram.sh (Диагностика)

Полная диагностика состояния Telegram бота.

```bash
# На сервере
bash scripts/diagnose-telegram.sh
```

Проверяет:
- Доступность бота
- Статус webhook
- Доступность endpoint
- Логи Docker
- Переменные окружения

### check-telegram-webhook.sh

Интерактивная проверка и настройка webhook для Linux/Mac.

```bash
bash scripts/check-telegram-webhook.sh
```

### check-telegram-webhook.ps1 (Windows)

Версия для Windows PowerShell.

```powershell
.\scripts\check-telegram-webhook.ps1
```

**Диагностика проблем с Telegram:**

Если бот не отвечает:

1. **Быстрое исправление:**
   ```bash
   bash scripts/fix-telegram-now.sh
   ```

2. **Полная диагностика:**
   ```bash
   bash scripts/diagnose-telegram.sh
   ```

3. **Проверка логов:**
   ```bash
   docker-compose logs -f app | grep WEBHOOK
   ```

4. **См. полную документацию:** [TELEGRAM_DEBUG.md](../TELEGRAM_DEBUG.md)

---

## 🛠️ Вспомогательные скрипты

### backup-database.ps1
Ручное создание бэкапа базы данных

```powershell
.\scripts\backup-database.ps1
```

---

### Восстановление базы данных

Восстановление выполняется вручную на сервере:

```bash
# Подключитесь к серверу
ssh root@your-server.com

# Восстановите бэкап
cd /opt/fb-net
cat backups/db_backup_20260204_120000.sql | docker compose -f docker-compose.production.yml exec -T postgres psql -U postgres -d postgres
```

---

## 🚀 Типичный рабочий процесс

```powershell
# 1. Внесли изменения в код

# 2. Коммит и push в GitHub
.\scripts\commit-and-push.ps1 -Message "Добавил новую функцию"

# 3. Быстрый деплой на сервер
.\scripts\deploy-from-github.ps1 -AppOnly

# Готово! ✅
```

---

## 📚 Подробная документация

См. [DEPLOY_GUIDE.md](../DEPLOY_GUIDE.md) для полной документации по деплою.
