# Скрипты проекта

## 🧹 Очистка кешей

### На сервере (Linux)

```bash
# Перейти в директорию проекта
cd /opt/fb-net  # или где у вас проект

# Сделать скрипт исполняемым (первый раз)
chmod +x scripts/clear-server-caches.sh

# Очистить все кеши
./scripts/clear-server-caches.sh

# Очистить кеши + пересобрать и перезапустить контейнеры
./scripts/clear-server-caches.sh --rebuild
```

### На локальной машине (Windows)

```powershell
# Очистить все кеши
.\scripts\clear-all-caches.ps1
```

## 📋 Что делает скрипт очистки на сервере

1. **Останавливает контейнеры** проекта
2. **Очищает Docker кеши:**
   - Неиспользуемые образы
   - Остановленные контейнеры
   - Неиспользуемые тома
   - Build кеш
3. **Очищает nginx кеши** (если используется)
4. **Очищает Next.js кеши** внутри контейнеров
5. **Очищает локальные кеши** проекта (`.next`, `.turbo` и т.д.)
6. **Пересобирает и перезапускает** контейнеры (если указан флаг `--rebuild`)

## ⚠️ Важные замечания

- Скрипт **НЕ удаляет** используемые тома (база данных сохранится)
- Скрипт автоматически определяет конфигурацию (SSL или обычная)
- При использовании `--rebuild` контейнеры будут пересобраны без кеша

## 🔍 Проверка после очистки

```bash
# Проверить статус контейнеров
docker-compose -f docker-compose.ssl.yml ps

# Посмотреть логи
docker-compose -f docker-compose.ssl.yml logs -f

# Проверить сайт
curl -I https://fibroadenoma.net
```

## 📚 Другие скрипты

- `setup-server-dependencies.sh` - установка зависимостей сервера
- `setup-ssl.sh` - настройка SSL сертификата
- `apply-migrations.sh` - применение миграций БД
- `backup-database.ps1` - резервное копирование БД (Windows)
- `deploy-from-github.ps1` - деплой с GitHub (Windows)
