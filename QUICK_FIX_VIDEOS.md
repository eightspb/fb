# 🎬 Быстрое исправление: Видео для конференций

## ⚡ Что нужно сделать (2 команды)

```powershell
# 1. Закоммитить изменения
.\scripts\commit-and-push.ps1 -Message "Fix: добавлена поддержка видео для конференций"

# 2. Деплой на сервер (миграция применится автоматически)
.\scripts\deploy-from-github.ps1
```

**Важно:** Используйте полный деплой **БЕЗ** флага `-AppOnly`!

## ✅ Что было исправлено

1. ✅ Создана миграция БД: `migrations/004_add_videos_to_conferences.sql`
2. ✅ Обновлены API роуты для сохранения видео
3. ✅ Обновлена схема БД
4. ✅ Frontend уже был готов (ConferenceForm, ConferenceVideos)

## 🔍 Проверка после деплоя

1. Откройте админ панель: https://fibroadenoma.net/admin
2. Перейдите в "Мероприятия" → Создать/Редактировать
3. Должна появиться секция **"Видео с предыдущих мероприятий"**
4. Добавьте тестовое видео и сохраните
5. Откройте страницу конференции - видео должно отображаться

## 📁 Что изменилось

### Новые файлы:
- `migrations/004_add_videos_to_conferences.sql`
- `migrations/README.md`
- `scripts/apply-migrations.sh`
- `FIX_VIDEOS_INSTRUCTIONS.md` (подробная инструкция)
- `MIGRATION_GUIDE.md` (руководство по миграциям)
- `CHANGELOG_VIDEOS.md` (полный список изменений)

### Изменённые файлы:
- `database-schema.sql` - добавлена колонка `videos`
- `src/app/api/conferences/route.ts` - обработка `videos` при создании
- `src/app/api/conferences/[id]/route.ts` - обработка `videos` при обновлении
- `docker-compose.yml` - монтаж папки `migrations` (локальная разработка)
- `docker-compose.production.yml` - монтаж папки `migrations`
- `docker-compose.ssl.yml` - монтаж папки `migrations`

## 🎯 Как работает

### В админ панели:
- Секция "Видео с предыдущих мероприятий"
- Кнопка "Добавить видео"
- Поля: название, путь к файлу, длительность
- Изменение порядка (стрелки вверх/вниз)

### На странице конференции:
- Секция отображается только если есть видео
- Видео плеер для каждого видео
- Название и длительность под плеером

### Формат данных:
```json
{
  "id": "video-1",
  "title": "Отчетное видео",
  "video_url": "/videos/conferences/video.mp4",
  "duration": "5:30",
  "order": 1
}
```

## 🚨 Если что-то не работает

### Видео не сохраняются:
```bash
# Проверьте, применена ли миграция
ssh root@155.212.217.60
cd /opt/fb-net
docker compose -f docker-compose.production.yml exec postgres psql -U postgres -d postgres -c "\d conferences" | grep videos
```

Должна быть строка с `videos | jsonb`

### Применить миграцию вручную:
```bash
# На сервере
cd /opt/fb-net
docker compose -f docker-compose.production.yml exec -T postgres psql -U postgres -d postgres < migrations/004_add_videos_to_conferences.sql
```

## 📞 Дополнительная помощь

- **Подробная инструкция**: `FIX_VIDEOS_INSTRUCTIONS.md`
- **Руководство по миграциям**: `MIGRATION_GUIDE.md`
- **Полный список изменений**: `CHANGELOG_VIDEOS.md`

## ✨ Готово!

После деплоя функционал видео будет полностью работать! 🎉
