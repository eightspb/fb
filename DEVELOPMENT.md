# Руководство разработчика FB.NET

## 📋 Содержание

1. [API и структура данных](#api-и-структура-данных)
2. [Компоненты](#компоненты)
3. [Работа с Supabase](#работа-с-supabase)
4. [Docker и локальная разработка](#docker-и-локальная-разработка)
5. [Стили и дизайн-система](#стили-и-дизайн-система)
6. [Утилитарные скрипты](#утилитарные-скрипты)

---

## API и структура данных

### NewsItem Interface

```typescript
interface NewsItem {
  id: string;
  title: string;
  shortDescription: string;
  fullDescription: string;
  date: string;           // Формат: "DD.MM.YYYY"
  year: string;           // Формат: "YYYY"
  images?: string[];
  videos?: string[];
  documents?: string[];
  tags?: string[];
  author?: string;
  location?: string;
  category?: string;
}
```

### Источники данных

**Текущее состояние**: Приложение использует статические данные из `src/lib/news-data.ts`

**Целевое состояние**: Миграция на Supabase через `src/lib/supabase-news.ts`

### Функции работы с данными

```typescript
// Статические данные (текущее)
import { newsData, getNewsById, getAllYears } from '@/lib/news-data';

// Supabase (будущее)
import { getAllNews, getNewsById } from '@/lib/supabase-news';
```

---

## Компоненты

### Переиспользуемые компоненты

#### Header
```tsx
import { Header } from "@/components/Header";
<Header />
```
- Адаптивная навигация
- Мобильное меню
- Логотип компании

#### Breadcrumbs
```tsx
import { Breadcrumbs } from "@/components/Breadcrumbs";
<Breadcrumbs items={[
  { label: "Главная", href: "/" },
  { label: "Новости" }
]} />
```

#### NewsList
```tsx
import { NewsList } from "@/components/NewsList";
<NewsList initialYear="2024" initialCategory="Мероприятия" />
```
- Фильтрация по годам и категориям
- Пагинация
- Адаптивный дизайн

### UI компоненты (shadcn/ui)

#### Button
```tsx
import { Button } from "@/components/ui/button";
<Button size="lg" variant="outline">Текст</Button>
```

#### Card
```tsx
import { Card, CardContent } from "@/components/ui/card";
<Card className="card-hover">
  <CardContent className="card-content">Контент</CardContent>
</Card>
```

#### Badge
```tsx
import { Badge } from "@/components/ui/badge";
<Badge variant="secondary">Метка</Badge>
```

---

## Работа с Supabase

### Полная автоматическая инициализация

**Главный скрипт для всего:**
```bash
npm run setup
```

Этот скрипт автоматически выполнит все необходимые шаги.

### Ручная настройка (если нужно)

**Вариант 1: Supabase CLI (рекомендуется)**
```bash
# Установка
npm install -g supabase

# Инициализация и запуск
npm run setup:supabase

# Полная настройка
npm run setup
```

**Вариант 2: Docker Compose**
```bash
# Запуск контейнеров и инициализация БД
npm run setup:docker

# Полная настройка
npm run setup
```

**Вариант 3: Облачный Supabase**
1. Создайте проект на [supabase.com](https://supabase.com)
2. Выполните SQL из `supabase-schema.sql` в SQL Editor
3. Создайте `.env.local` с ключами
4. Запустите `npm run setup`

### Схема базы данных

**Файл**: `supabase-schema.sql`

Таблицы:
- `news` - основная таблица новостей
- `news_images` - изображения новостей
- `news_tags` - теги новостей
- `news_videos` - видео новостей
- `news_documents` - документы новостей

### Миграция данных

После выполнения `npm run setup` данные автоматически мигрируются. Вручную:

```bash
# Мигрировать из news-data.ts в Supabase
npm run migrate:news

# Создать новости из папок с фотографиями
npm run create:news-from-folders
```

### Переменные окружения

Создайте `.env.local`:
```env
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

**Откуда взять ключи:**
- **Supabase CLI**: из вывода `supabase start`
- **Docker**: значения из `docker-compose.simple.yml`
- **Облачный**: Settings → API в Supabase Dashboard

---

## Telegram Bot интеграция

### Архитектура

Бот работает через webhook endpoint `/api/telegram/webhook`, который принимает обновления от Telegram и обрабатывает их.

### Основные компоненты

- `src/lib/telegram-bot.ts` - основной обработчик сообщений
- `src/lib/openrouter.ts` - интеграция с OpenRouter для расширения текста
- `src/lib/telegram-notifications.ts` - уведомления администратору
- `src/lib/file-utils.ts` - утилиты для работы с файлами
- `src/app/api/telegram/webhook/route.ts` - webhook endpoint

### Процесс создания новости

1. Пользователь отправляет сообщения боту (текст, фото, видео)
2. Бот сохраняет данные во временное хранилище
3. При команде `/done`:
   - Скачиваются медиафайлы из Telegram
   - Файлы сохраняются локально в `public/images/trainings/YYYY.MM.DD/`
   - Текст расширяется через OpenRouter (ChatGPT)
   - Создается черновик новости в БД со статусом `draft`
   - Отправляется уведомление администратору

4. Администратор получает уведомление с кнопками:
   - "Опубликовать" - меняет статус на `published`
   - "Отклонить" - удаляет новость
   - "Просмотреть" - открывает страницу новости

### Настройка для разработки

Для локальной разработки можно использовать:
- [ngrok](https://ngrok.com) для создания туннеля к localhost
- Или использовать polling режим вместо webhook (требует изменений в коде)

### Переменные окружения

```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_ADMIN_CHAT_ID=your_chat_id
OPENROUTER_API_KEY=your_openrouter_key
TELEGRAM_WEBHOOK_URL=https://your-domain.com/api/telegram/webhook
```

---

## Docker и локальная разработка

### Быстрый старт

```bash
# Запустить все сервисы
npm run docker:up

# Просмотр логов
npm run docker:logs

# Остановить
npm run docker:down
```

### Доступные сервисы

- **Next.js**: http://localhost:3000
- **PostgreSQL**: localhost:54322
- **Supabase Studio**: http://localhost:54323 (если используется полный стек)

### Docker Compose файлы

- `docker-compose.simple.yml` - упрощенная версия (PostgreSQL + Next.js)
- `docker-compose.yml` - полный стек Supabase

### Полезные команды

```bash
# Пересобрать контейнеры
docker-compose -f docker-compose.simple.yml build --no-cache

# Очистить все данные
docker-compose -f docker-compose.simple.yml down -v

# Войти в контейнер Next.js
docker exec -it fb-net-nextjs sh

# Войти в PostgreSQL
docker exec -it fb-net-supabase-db psql -U postgres -d postgres
```

---

## Стили и дизайн-система

### Tailwind CSS

Проект использует Tailwind CSS для утилитарных классов:
```tsx
<div className="flex items-center gap-4 p-6 bg-white rounded-lg">
```

### Кастомные CSS классы

**Файл**: `src/styles/components.css`

#### Контейнеры страниц
```css
.page-container          /* Основной контейнер */
.page-max-width          /* Ограничение ширины */
.page-max-width-wide     /* Широкий контейнер */
.page-title              /* Заголовки страниц */
```

#### Компоненты
```css
.card-hover              /* Hover эффект для карточек */
.card-content            /* Содержимое карточек */
.header-*                /* Элементы шапки */
.news-*                  /* Компоненты новостей */
```

#### Адаптивность
```css
/* Мобильная версия */
@media (max-width: 767px) {
  .news-grid { /* ... */ }
  .sidebar-card { /* ... */ }
}
```

### Цветовая палитра

- Основной: `#2563eb` (blue-500)
- Фон: `#e0e0e0` (серый)
- Текст: `#1f2937` (gray-800)

---

## Утилитарные скрипты

### 1. Организация изображений по датам

**Скрипт**: `scripts/organize-images-by-date.ts`

Организует изображения в папке `public/images/trainings` по датам съемки из EXIF данных.

```bash
npm run organize:images
```

**Что делает**:
- Читает EXIF данные из изображений
- Создает папки в формате `YYYY.MM.DD`
- Перемещает файлы в соответствующие папки

### 2. Переименование папок с датами

**Скрипт**: `scripts/rename-date-folders.ts`

Переименовывает папки из формата `DD.MM.YYYY` в `YYYY.MM.DD`.

```bash
npm run rename:folders
```

### 3. Создание новостей из папок

**Скрипт**: `scripts/create-news-from-folders.ts`

Создает новости в Supabase из папок с фотографиями.

```bash
npm run create:news-from-folders
```

**Что делает**:
- Сканирует папки с датами в формате `YYYY.MM.DD`
- Группирует последовательные даты (2+ дня подряд) в одно событие
- Извлекает геолокацию из EXIF данных
- Создает новости с изображениями, видео и геолокацией

**Особенности**:
- Автоматическая группировка последовательных дат
- Извлечение GPS координат из изображений
- Валидация данных перед вставкой
- Обработка ошибок на всех этапах

### 4. Миграция новостей в Supabase

**Скрипт**: `scripts/migrate-news-to-supabase.ts`

Мигрирует данные из `src/lib/news-data.ts` в Supabase.

```bash
npm run migrate:news
```

---

## Архитектурные решения

### 1. Next.js App Router
- Файловая система маршрутизации
- Серверные компоненты по умолчанию
- Оптимизация производительности

### 2. Компонентный подход
- Переиспользуемые компоненты
- Составные компоненты (shadcn/ui)
- Кастомные CSS классы для консистентности

### 3. Адаптивный дизайн
- Mobile-first подход
- Flexbox и Grid
- Медиа-запросы для разных экранов

### 4. SEO оптимизация
- Семантическая разметка
- Мета-теги для страниц
- Структурированные данные

---

## Добавление нового функционала

### Новая страница

1. Создать папку в `src/app/`
2. Добавить `page.tsx`
3. Использовать существующие компоненты
4. Применять CSS классы из `components.css`

### Новый компонент

1. Создать файл в `src/components/`
2. Использовать TypeScript интерфейсы
3. Применять CSS классы
4. Экспортировать компонент

### Новые стили

1. Добавить CSS класс в `src/styles/components.css`
2. Следовать существующему неймингу
3. Тестировать на разных экранах

---

## Соглашения по коду

### TypeScript
- Строгая типизация
- Интерфейсы для props
- Типы для API ответов

### CSS
- Семантические имена классов
- BEM-подобная методология
- Переменные для цветов и размеров

### Компоненты
- Функциональные компоненты
- Деструктуризация props
- Ранний return для условий

---

## Отладка и тестирование

### Ручное тестирование
- Адаптивность на разных устройствах
- Валидация форм
- Навигация по страницам
- Производительность загрузки

### Проверка подключения к БД
```bash
# Проверить подключение к Supabase
npm run docker:psql

# Или через Supabase CLI
supabase status
```

### Логи
```bash
# Логи Docker контейнеров
npm run docker:logs

# Логи Supabase
supabase logs
```

---

## Развертывание

### Vercel
1. Подключить репозиторий GitHub
2. Настроить переменные окружения
3. Автоматическая сборка и деплой

### Netlify
1. Build command: `npm run build`
2. Publish directory: `.next`
3. Настроить переменные окружения

### Переменные окружения для продакшна

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key
```

---

## Полезные ссылки

- [Next.js Documentation](https://nextjs.org/docs)
- [Supabase Documentation](https://supabase.com/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [shadcn/ui](https://ui.shadcn.com)

