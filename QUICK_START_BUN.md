# ⚡ Быстрый старт с Bun

## 🎯 Миграция за 3 минуты

### ⚠️ ВАЖНО: Миграция выполняется НА СЕРВЕРЕ автоматически!

### Одна команда для полной миграции:

```powershell
# Windows
.\scripts\deploy-and-migrate-bun.ps1 -Server user@server -RemotePath /opt/fb-net

# Linux/macOS
./scripts/deploy-and-migrate-bun.sh user@server /opt/fb-net
```

Скрипт автоматически выполнит НА СЕРВЕРЕ:
- ✅ Создаст бэкапы БД и файлов
- ✅ Установит Bun
- ✅ Мигрирует на Bun
- ✅ Пересоберет Docker контейнеры
- ✅ Запустит приложение

## 📋 Основные команды (выполняются НА СЕРВЕРЕ через SSH)

```bash
# SSH на сервер
ssh user@server

# Перейти в папку проекта
cd /opt/fb-net

# Проверить установленные пакеты
~/.bun/bin/bun pm ls

# Обновить зависимости (если нужно)
~/.bun/bin/bun update

# Перезапустить контейнеры
docker compose -f docker-compose.production.yml restart
```

## 🚀 Повторный деплой (если нужны изменения)

```powershell
# Windows
.\scripts\deploy-and-migrate-bun.ps1 -Server user@server

# Linux/macOS
./scripts/deploy-and-migrate-bun.sh user@server
```

## 📈 Сравнение производительности

| Команда       | npm     | Bun    | Ускорение |
|---------------|---------|--------|-----------|
| install       | 45 сек  | 3 сек  | **15x**   |
| Docker build  | 4 мин   | 40 сек | **6x**    |

## 💡 Советы

- **bun.lockb** - создается автоматически НА СЕРВЕРЕ
- **Бэкапы** - все бэкапы хранятся НА СЕРВЕРЕ в `$REMOTE_PATH/backups/`
- **Совместимость** - все npm пакеты работают с Bun
- **Откат** - восстановление из бэкапов НА СЕРВЕРЕ

## 📖 Подробная документация

См. [BUN_MIGRATION.md](./BUN_MIGRATION.md) для полной инструкции
