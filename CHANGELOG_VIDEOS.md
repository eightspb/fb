# Changelog: Поддержка видео для конференций

## Дата: 2026-02-06

### 🎯 Цель
Добавить возможность прикреплять видео с предыдущих мероприятий к конференциям.

### 📝 Изменения

#### 1. База данных

**Новые файлы:**
- `migrations/004_add_videos_to_conferences.sql` - миграция для добавления колонки `videos`
- `migrations/README.md` - документация по миграциям

**Изменённые файлы:**
- `database-schema.sql` - добавлена колонка `videos JSONB DEFAULT '[]'` в таблицу `conferences`

#### 2. Backend (API)

**Изменённые файлы:**
- `src/app/api/conferences/route.ts`:
  - Добавлена обработка поля `videos` в POST запросе
  - Видео сохраняются в БД при создании конференции

- `src/app/api/conferences/[id]/route.ts`:
  - Добавлена обработка поля `videos` в PUT запросе
  - Видео обновляются в БД при редактировании конференции

#### 3. Frontend (уже был реализован)

Компоненты уже были готовы:
- `src/components/admin/ConferenceForm.tsx` - форма с секцией для управления видео
- `src/components/ConferenceVideos.tsx` - компонент для отображения видео
- `src/app/conferences/[id]/page.tsx` - страница конференции с видео

#### 4. Docker

**Изменённые файлы:**
- `docker-compose.yml` - добавлен монтаж папки `migrations` в контейнер БД (для локальной разработки)
- `docker-compose.production.yml` - добавлен монтаж папки `migrations` в контейнер БД
- `docker-compose.ssl.yml` - добавлен монтаж папки `migrations` в контейнер БД

**Новые файлы:**
- `scripts/apply-migrations.sh` - скрипт для ручного применения миграций

#### 5. Документация

**Новые файлы:**
- `FIX_VIDEOS_INSTRUCTIONS.md` - инструкция по применению исправления
- `MIGRATION_GUIDE.md` - подробное руководство по миграциям
- `CHANGELOG_VIDEOS.md` - этот файл

### 🔄 Процесс деплоя

```powershell
# 1. Коммит изменений
.\scripts\commit-and-push.ps1 -Message "Fix: добавлена поддержка видео для конференций"

# 2. Полный деплой (миграция применится автоматически)
.\scripts\deploy-from-github.ps1
```

### ✅ Что работает после применения

1. **В админ панели** (`/admin/conferences/create` или `/admin/conferences/[id]`):
   - Секция "Видео с предыдущих мероприятий"
   - Возможность добавлять/редактировать/удалять видео
   - Поля: название, путь к файлу, длительность
   - Изменение порядка отображения видео

2. **На странице конференции** (`/conferences/[id]`):
   - Секция "Видео с предыдущих мероприятий"
   - Видео плеер для каждого видео
   - Отображение названия и длительности

3. **API**:
   - POST `/api/conferences` - сохраняет видео при создании
   - PUT `/api/conferences/[id]` - обновляет видео при редактировании
   - GET `/api/conferences/[id]` - возвращает видео в ответе

### 📊 Формат данных

Видео хранятся в колонке `videos` типа JSONB:

```json
[
  {
    "id": "string",
    "title": "string",
    "video_url": "string",
    "duration": "string | null",
    "order": number
  }
]
```

Пример:
```json
[
  {
    "id": "video-1",
    "title": "Отчетное видео с конференции 2025",
    "video_url": "/videos/conferences/sms2_report.mp4",
    "duration": "8:45",
    "order": 1
  }
]
```

### 🔒 Обратная совместимость

- Существующие конференции без видео продолжат работать (videos = [])
- Старые конференции можно отредактировать и добавить видео
- Если у конференции нет видео, секция не отображается на странице

### 🎯 Следующие шаги

1. Применить миграцию на сервере (автоматически при деплое)
2. Протестировать создание/редактирование конференций с видео
3. Загрузить реальные видео файлы в `public/videos/conferences/`
4. Добавить видео к существующим конференциям через админ панель

### 📝 Примечания

- Видео файлы должны быть размещены в `public/videos/conferences/` или другом доступном месте
- Путь к видео должен быть относительным от корня сайта (начинается с `/`)
- Рекомендуемый формат: MP4 для максимальной совместимости с браузерами
- Миграция применяется автоматически при полном деплое через `deploy-from-github.ps1`
