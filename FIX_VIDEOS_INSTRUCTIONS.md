# Инструкция по исправлению проблемы с видео конференций

## 🔍 Проблема

Видео не отображаются на странице конференции и не сохраняются в админ панели.

## ✅ Что было исправлено

1. **Создана миграция базы данных**: `migrations/004_add_videos_to_conferences.sql`
   - Добавляет колонку `videos` (JSONB) в таблицу `conferences`

2. **Обновлены API роуты**:
   - `src/app/api/conferences/route.ts` - теперь сохраняет видео при создании конференции
   - `src/app/api/conferences/[id]/route.ts` - теперь сохраняет видео при обновлении конференции

3. **Обновлена схема БД**: `database-schema.sql` - добавлена колонка `videos`

4. **Обновлен docker-compose**: `docker-compose.production.yml` - добавлен монтаж папки `migrations`

## 🚀 Как применить исправление

### Вариант 1: Автоматический деплой (РЕКОМЕНДУЕТСЯ)

```powershell
# 1. Закоммитить и запушить изменения в GitHub
.\scripts\commit-and-push.ps1 -Message "Fix: добавлена поддержка видео для конференций"

# 2. Деплой на сервер (миграция применится автоматически)
.\scripts\deploy-from-github.ps1
```

**Важно**: Используйте **полный деплой** без флага `-AppOnly`, чтобы миграция применилась!

### Вариант 2: Ручное применение миграции на сервере

Если вы хотите применить только миграцию без полного деплоя:

```bash
# Подключитесь к серверу
ssh root@155.212.217.60

# Перейдите в директорию проекта
cd /opt/fb-net

# Примените миграцию вручную
docker compose -f docker-compose.production.yml exec -T postgres psql -U postgres -d postgres < migrations/004_add_videos_to_conferences.sql

# Или используйте скрипт
bash scripts/apply-migrations.sh migrations/004_add_videos_to_conferences.sql
```

## ✅ Проверка

После применения миграции проверьте:

### 1. Проверка колонки в БД

```bash
# На сервере
docker compose -f docker-compose.production.yml exec postgres psql -U postgres -d postgres -c "\d conferences"
```

Вы должны увидеть колонку `videos` типа `jsonb`.

### 2. Проверка в админ панели

1. Откройте админ панель: https://fibroadenoma.net/admin
2. Перейдите в раздел "Мероприятия"
3. Создайте или отредактируйте конференцию
4. Должна появиться секция **"Видео с предыдущих мероприятий"**
5. Добавьте видео с полями:
   - Название: "Тестовое видео"
   - Путь к файлу: `/videos/test.mp4`
   - Длительность: "5:30"
6. Сохраните конференцию

### 3. Проверка на странице конференции

1. Откройте страницу конференции
2. Если у конференции есть видео, они должны отображаться в секции "Видео с предыдущих мероприятий"

## 📊 Структура данных видео

Видео хранятся в формате JSONB:

```json
[
  {
    "id": "video-1",
    "title": "Отчетное видео с конференции 2025",
    "video_url": "/videos/conferences/sms2_report.mp4",
    "duration": "8:45",
    "order": 1
  }
]
```

## 🎯 Важные замечания

1. **Видео файлы** должны быть размещены в папке `public/videos/conferences/` или другом доступном месте
2. **Путь к видео** должен быть относительным от корня сайта (начинается с `/`)
3. **Формат видео** - рекомендуется MP4 для максимальной совместимости
4. **Обратная совместимость** - существующие конференции без видео будут работать нормально

## 🔧 Если что-то пошло не так

### Ошибка: "column videos does not exist"

Миграция не была применена. Примените её вручную (см. Вариант 2 выше).

### Видео не сохраняются

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте логи приложения:
   ```bash
   docker compose -f docker-compose.production.yml logs app --tail=50
   ```
3. Убедитесь, что миграция применена (см. раздел "Проверка")

### Видео не отображаются

1. Убедитесь, что видео файлы существуют по указанным путям
2. Проверьте данные в БД:
   ```sql
   SELECT id, title, videos FROM conferences WHERE videos IS NOT NULL AND videos != '[]'::jsonb;
   ```
3. Проверьте консоль браузера на ошибки загрузки видео

## 📝 Следующие шаги

После применения исправления:

1. Протестируйте создание конференции с видео
2. Протестируйте редактирование существующей конференции
3. Убедитесь, что видео отображаются на странице конференции
4. Загрузите реальные видео файлы в папку `public/videos/conferences/`

## 🎉 Готово!

После выполнения всех шагов функционал видео для конференций будет полностью работать.
