# Bun runtime configuration

[install]
# Автоматически устанавливать peer dependencies
auto = true

# Использовать точные версии (как bun install --frozen-lockfile)
exact = false

# Кеш
cache = true

[test]
# Настройки для тестов (если будут добавлены)
preload = []
