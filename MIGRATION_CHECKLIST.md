# ✅ Чеклист миграции на Bun

Используйте этот чеклист для пошагового выполнения миграции.

## ⚠️ ВАЖНО: ВСЯ МИГРАЦИЯ ВЫПОЛНЯЕТСЯ НА СЕРВЕРЕ!

## Подготовка

- [ ] Проверен SSH доступ к серверу: `ssh user@server`
- [ ] Все локальные изменения закоммичены в git (если есть)
- [ ] Проверен путь к проекту на сервере (обычно `/opt/fb-net`)

## Запуск миграции НА СЕРВЕРЕ

- [ ] Запущена ОДНА команда миграции:
  - Windows: `.\scripts\deploy-and-migrate-bun.ps1 -Server user@server -RemotePath /opt/fb-net`
  - Linux: `./scripts/deploy-and-migrate-bun.sh user@server /opt/fb-net`

## Автоматические проверки скрипта (происходят на сервере)

- [ ] ✅ Подключение к серверу установлено
- [ ] ✅ Создан бэкап БД НА СЕРВЕРЕ в `backups/`
- [ ] ✅ Создан бэкап package-lock.json НА СЕРВЕРЕ
- [ ] ✅ Файлы загружены на сервер через rsync
- [ ] ✅ Bun установлен НА СЕРВЕРЕ (или уже был установлен)
- [ ] ✅ Удалены node_modules НА СЕРВЕРЕ
- [ ] ✅ Установлены зависимости с Bun НА СЕРВЕРЕ
- [ ] ✅ Все критические зависимости проверены НА СЕРВЕРЕ
- [ ] ✅ Создан файл `bun.lockb` НА СЕРВЕРЕ
- [ ] ✅ Применены миграции БД НА СЕРВЕРЕ
- [ ] ✅ Docker контейнеры пересобраны с Bun НА СЕРВЕРЕ
- [ ] ✅ Контейнеры запущены НА СЕРВЕРЕ
- [ ] ✅ Проверка здоровья приложения выполнена

## Проверка работы

- [ ] Сайт доступен на production URL
- [ ] Главная страница открывается
- [ ] Админ панель доступна и работает
- [ ] Формы отправки работают
- [ ] Карты отображаются
- [ ] Изображения загружаются
- [ ] База данных работает корректно

## Commit изменений (опционально)

- [ ] Закоммичены обновленные файлы локально (если были изменения):
  ```bash
  git add .
  git commit -m "Migrate to Bun on server for 10x faster deployments"
  git push
  ```

## Проверка работы

- [ ] Главная страница открывается
- [ ] Админ панель доступна и работает
- [ ] Формы отправки работают
- [ ] Карты отображаются
- [ ] Изображения загружаются
- [ ] База данных работает корректно

## Документация

- [ ] Прочитана документация [BUN_MIGRATION.md](./BUN_MIGRATION.md)
- [ ] Команда знает новые команды Bun
- [ ] Обновлены CI/CD скрипты (если есть)

## Очистка

- [ ] Удалены старые бэкапы из папки `backups/` (опционально)
- [ ] Проверены логи Docker на наличие ошибок
- [ ] Мониторинг показывает нормальную работу

## В случае проблем

Если что-то пошло не так, ВСЕ ОПЕРАЦИИ ВЫПОЛНЯЮТСЯ НА СЕРВЕРЕ:

1. **Откат на npm НА СЕРВЕРЕ**:
   ```bash
   ssh user@server
   cd /opt/fb-net
   rm -rf node_modules bun.lockb
   cp backups/package-lock.json.backup_* package-lock.json
   docker compose -f docker-compose.production.yml down
   docker compose -f docker-compose.production.yml build --no-cache
   docker compose -f docker-compose.production.yml up -d
   ```

2. **Восстановление базы данных НА СЕРВЕРЕ**:
   ```bash
   ssh user@server
   cd /opt/fb-net
   cat backups/db_backup_TIMESTAMP.sql | docker exec -i $(docker compose -f docker-compose.production.yml ps -q postgres) psql -U postgres -d postgres
   ```

3. **Проверка логов НА СЕРВЕРЕ**:
   ```bash
   ssh user@server
   cd /opt/fb-net
   docker compose -f docker-compose.production.yml logs -f
   ```

## 🎉 Миграция завершена!

После завершения всех пунктов:

✅ Ваш проект работает на Bun  
✅ Установка пакетов ускорена в 10+ раз  
✅ Docker build ускорен в 6+ раз  
✅ Deployment работает корректно  

---

**Дата миграции**: _______________  
**Ответственный**: _______________  
**Версия Bun**: _______________
