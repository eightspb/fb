# Компоненты FB.NET

## 📋 Обзор

Этот документ содержит подробное описание всех компонентов проекта FB.NET, их API, примеры использования и архитектурные решения.

## 🏗 Архитектура компонентов

### Типы компонентов

1. **Переиспользуемые компоненты** (`src/components/`)
   - Header, Breadcrumbs, Footer
   - Глобальные компоненты сайта

2. **UI компоненты** (`src/components/ui/`)
   - Button, Card, Badge, etc.
   - Компоненты из shadcn/ui

3. **Страничные компоненты** (`src/app/*/page.tsx`)
   - Компоненты конкретных страниц
   - Локальная логика

## 🧩 Переиспользуемые компоненты

### Header

**Расположение**: `src/components/Header.tsx`

**Описание**: Основная навигационная панель сайта с адаптивным меню.

**Props**: Нет

**Использование**:
```tsx
import { Header } from "@/components/Header";

// В layout или на странице
<Header />
```

**Структура**:
```tsx
<header className="header">
  <div className="header-container header-nav">
    <div className="header-logo">FB.NET</div>
    <nav className="header-menu">
      {/* Навигационные ссылки */}
    </nav>
    <div className="header-mobile-menu">
      {/* Мобильное меню */}
    </div>
  </div>
</header>
```

**CSS классы**:
- `.header` - Основной контейнер
- `.header-container` - Flex контейнер
- `.header-nav` - Навигационная панель
- `.header-logo` - Логотип
- `.header-menu` - Десктопное меню
- `.header-menu-link` - Ссылки меню
- `.header-mobile-menu` - Мобильное меню

### Breadcrumbs

**Расположение**: `src/components/Breadcrumbs.tsx`

**Описание**: Компонент хлебных крошек для навигации по сайту.

**Props**:
```typescript
interface BreadcrumbItem {
  label: string;
  href?: string;
}

interface BreadcrumbsProps {
  items: BreadcrumbItem[];
}
```

**Использование**:
```tsx
import { Breadcrumbs } from "@/components/Breadcrumbs";

<Breadcrumbs items={[
  { label: "Главная", href: "/" },
  { label: "Оборудование", href: "/equipment" },
  { label: "Система DK-B-MS" }
]} />
```

**CSS классы**:
- `.breadcrumb` - Контейнер
- `.breadcrumb-list` - Список элементов
- `.breadcrumb-link` - Ссылки
- `.breadcrumb-current` - Текущая страница

## 🎨 UI компоненты (shadcn/ui)

### Button

**Описание**: Кастомизированная кнопка на основе Radix UI.

**Варианты**:
- `default` - Основной стиль
- `outline` - Контурный стиль
- `secondary` - Вторичный стиль
- `ghost` - Прозрачный стиль
- `link` - Стиль ссылки

**Размеры**:
- `sm` - Маленький
- `default` - Средний
- `lg` - Большой

**Использование**:
```tsx
import { Button } from "@/components/ui/button";

<Button variant="outline" size="lg">
  Нажми меня
</Button>

<Button asChild>
  <Link href="/contacts">Ссылка</Link>
</Button>
```

### Card

**Описание**: Контейнер для группировки контента.

**Состав**:
- `Card` - Основной контейнер
- `CardHeader` - Заголовок карточки
- `CardContent` - Основной контент
- `CardFooter` - Нижняя часть

**Использование**:
```tsx
import { Card, CardContent, CardHeader } from "@/components/ui/card";

<Card className="card-hover">
  <CardHeader>
    <h3>Заголовок</h3>
  </CardHeader>
  <CardContent className="card-content">
    Контент карточки
  </CardContent>
</Card>
```

### Badge

**Описание**: Метки для категоризации контента.

**Варианты**:
- `default` - Основной
- `secondary` - Вторичный
- `outline` - Контурный

**Использование**:
```tsx
import { Badge } from "@/components/ui/badge";

<Badge variant="secondary">Категория</Badge>
<Badge>Год: 2024</Badge>
```

## 📄 Страничные компоненты

### Структура страниц

Все страницы следуют единому паттерну:

```tsx
export default function PageName() {
  return (
    <div className="min-h-screen bg-white">
      <Header />

      <main className="page-container">
        <div className="page-max-width-wide">
          <Breadcrumbs items={[...]} />

          <h1 className="page-title">Заголовок страницы</h1>

          {/* Секции страницы */}
        </div>
      </main>

      {/* Footer */}
    </div>
  );
}
```

### Главная страница (`/`)

**Секции**:
1. Hero секция с преимуществами
2. Социальные доказательства
3. Видео демонстрация
4. Ближайшие события
5. Академия
6. Footer

### Страница пациентов (`/patients`)

**Секции**:
1. Введение в ВАБ
2. Процесс процедуры (6 шагов)
3. Преимущества ВАБ (6 карточек)
4. Этапы проведения (4 этапа)
5. FAQ
6. Показания и противопоказания
7. CTA секция

### Страница оборудования (`/equipment`)

**Секции**:
1. Введение в систему DK-B-MS
2. Технические преимущества (8 пунктов)
3. Процедура (4 шага)
4. Инновации
5. Спецификации
6. CTA секция

### Страница новостей (`/news`)

**Особенности**:
- Группировка по годам
- Архив с фильтрацией
- Динамические страницы новостей

## 🎨 Система стилей

### CSS классы по назначению

#### Layout классы
```css
.page-container      /* Основной контейнер страниц */
.page-max-width      /* Стандартная ширина (56rem) */
.page-max-width-wide /* Широкая ширина (80rem) */
.page-title          /* Заголовки страниц */
```

#### Компонентные классы
```css
.card-hover          /* Hover эффект для карточек */
.card-content        /* Стандартный padding для карточек */
.breadcrumb          /* Контейнер хлебных крошек */
.sidebar-card        /* Фиксированная боковая панель */
```

#### Специфические классы
```css
.equipment-purpose   /* Введение в оборудование */
.equipment-features  /* Особенности продукта */
.equipment-benefits  /* Преимущества */
.equipment-specifications /* Технические характеристики */

.hero               /* Hero секция */
.hero-container     /* Контейнер hero */
.hero-title         /* Заголовок hero */
.hero-subtitle      /* Подзаголовок hero */
.hero-buttons       /* Кнопки hero */
```

### Адаптивность

Все компоненты адаптивны с использованием медиа-запросов:

```css
/* Мобильные стили (по умолчанию) */
.header-menu {
  display: none;
}

/* Десктопные стили */
@media (min-width: 768px) {
  .header-menu {
    display: flex;
  }
}
```

## 🔧 Создание новых компонентов

### Шаблон компонента

```tsx
import { FC } from "react";

interface ComponentNameProps {
  // Определить props
  title?: string;
  children?: React.ReactNode;
}

export const ComponentName: FC<ComponentNameProps> = ({
  title,
  children
}) => {
  return (
    <div className="component-name">
      {title && <h3 className="component-name-title">{title}</h3>}
      <div className="component-name-content">
        {children}
      </div>
    </div>
  );
};
```

### Добавление стилей

1. Добавить CSS классы в `src/styles/components.css`
2. Использовать семантические имена
3. Добавить адаптивность

```css
.component-name {
  /* Базовые стили */
}

.component-name-title {
  /* Стили заголовка */
}

.component-name-content {
  /* Стили контента */
}

@media (min-width: 768px) {
  .component-name {
    /* Десктопные стили */
  }
}
```

## 📊 Производительность

### Оптимизации

1. **Серверные компоненты** - по умолчанию в Next.js 13+
2. **Lazy loading** - для тяжелых компонентов
3. **Image optimization** - использование Next.js Image
4. **CSS optimization** - минимальный размер бандла

### Метрики

- First Contentful Paint: < 1.5s
- Largest Contentful Paint: < 2.5s
- Cumulative Layout Shift: < 0.1

## 🧪 Тестирование компонентов

### Модульные тесты

```tsx
import { render, screen } from "@testing-library/react";
import { Button } from "@/components/ui/button";

describe("Button", () => {
  it("renders children", () => {
    render(<Button>Click me</Button>);
    expect(screen.getByText("Click me")).toBeInTheDocument();
  });
});
```

### E2E тестирование

Использовать Playwright или Cypress для тестирования пользовательских сценариев.

## 📚 Дополнительные ресурсы

- [Next.js Documentation](https://nextjs.org/docs)
- [shadcn/ui Documentation](https://ui.shadcn.com)
- [Tailwind CSS Documentation](https://tailwindcss.com)
- [Radix UI Documentation](https://radix-ui.com)
